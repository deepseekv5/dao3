# ── 不可再分发的第三方内容 ──────────────────────────────────────────
# 这些来自 dao3.fun 的内容服务（游戏素材与地图数据），版权归 box3lab 及其权利人。
# 例外一：赛车模板地图数据按仓库所有者的决定**随包分发**，但不在服务端自动落地——
# 首次启动由用户在应用内明确确认"我有权使用"后才安装（见 server.js 的 /api/consent）。
# 例外二：official-project/racing-assets/ 里的 20 个赛道模型与 40 个音效同样随包分发，
# 走同一道确认闸门；未确认前服务端对这两个前缀直接 403，客户端拿不到也不请求。
# public/assets 与 public/data/assets 从此只是运行时产物位置（世界上传件、取回脚本落点），
# 素材本体不在那里。
public/data/assets/
public/assets/
# official-project/ 里除了 racing-template/（660KB 的官方 .raw 原始响应，只是导入用的
# 中间产物，随时能按 CID 重新取回）之外都入库：cids.json、地图数据、以及素材包。
official-project/racing-template/
server/data/

# ── 上游参考仓库（Apache-2.0，但体积 200MB+，不镜像）─────────────────
# 顶层 vendor/ 里每个子目录都是各上游仓库自己的 clone（带 .git），作为嵌套仓库
# 无法、也不应入库。其中可再分发的两张小表已复制到 data/upstream/（随 LICENSE 一起走）。
#
# 必须写成 /vendor/：gitignore 的 "vendor/" 会匹配**任意层级**同名目录，
# 于是把随包分发的 MIT three.js（public/vendor/）也一起吞掉 —— 那会让 clone 出来的
# 仓库没有渲染器、整个应用白屏。锚定到根目录才不会误伤。
/vendor/

# ── 本地状态与产物 ──────────────────────────────────────────────────
tmp/
node_modules/
test/out/
# play/ 是 npm run build:play 的产物，推送到独立仓库 deepseekv5/dao3play 作 Pages 根。
# 源码在 play-src/ 与 scripts/build-play.mjs，必须入库；产物本身不入库（否则 three.js
# 会在 git 里存两份，且 2.8MB 地图 gz 每次改动都产生一份新 blob）。
play/
run.pid
run.out
*.log
.DS_Store

# ── 任何凭据 ────────────────────────────────────────────────────────
.env
.env.*
*token*
*令牌*
*.pem
*.key
