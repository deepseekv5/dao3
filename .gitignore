# ── 不可再分发的第三方内容 ──────────────────────────────────────────
# 这些来自 dao3.fun 的内容服务（游戏素材与地图数据），版权归 box3lab 及其权利人。
# 例外：赛车模板地图数据按仓库所有者的决定**随包分发**，但不在服务端自动落地——
# 首次启动由用户在应用内明确确认"我有权使用"后才安装（见 server.js 的 /api/consent）。
# 音效与赛道模型仍然排除，用 npm run fetch:official 按 CID 自行取回。
public/data/assets/
public/assets/
official-project/*
!official-project/cids.json
!official-project/racing-template.json.gz
server/data/

# ── 上游参考仓库（Apache-2.0，但体积 200MB+，不镜像）─────────────────
# 顶层 vendor/ 里每个子目录都是各上游仓库自己的 clone（带 .git），作为嵌套仓库
# 无法、也不应入库。其中可再分发的两张小表已复制到 data/upstream/（随 LICENSE 一起走）。
#
# 必须写成 /vendor/：gitignore 的 "vendor/" 会匹配**任意层级**同名目录，
# 于是把随包分发的 MIT three.js（public/vendor/）也一起吞掉 —— 那会让 clone 出来的
# 仓库没有渲染器、整个应用白屏。锚定到根目录才不会误伤。
/vendor/

# ── 本地状态与产物 ──────────────────────────────────────────────────
tmp/
node_modules/
test/out/
run.pid
run.out
*.log
.DS_Store

# ── 任何凭据 ────────────────────────────────────────────────────────
.env
.env.*
*token*
*令牌*
*.pem
*.key
