# 更新日志

本文件按版本倒序记录**你能感知到的变化**：修了什么问题（附现象）、加了什么能力、
素材分发策略改过几次以及为什么。实现细节请查 `git log` 与 `docs/`，这里不放提交信息复读。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)。

---

## [1.0.8] - 2026-09-23

### 修复

- **官方写法的脚本一进沙箱就整段不执行。** 脚本是用同步 `new Function` 编译的，
  而官方示例与真实地图脚本大量在**文件顶层直接写 `await`**
  （`const ds = storage.getDataStorage("x"); await ds.set(…)`）。结果是第一行 `await`
  就抛 `SyntaxError`、整个脚本一个字都不跑，而提示只有一行"await is only valid in async functions"。
  现在按 async 函数体编译（行号与 `sourceURL` 保持原样，编辑器里的错误定位不受影响），
  异步失败也会走回同一条报错路径，不会变成静默的 unhandled rejection。
- **`GameRGBColor` 的分量名和官方对不上。** 官方是 `r` / `g` / `b`（`GameRGBAColor` 多一个 `a`），
  我们内部却用 `red` / `green` / `blue`。脚本照官方写法取 `color.r` 会拿到 `undefined`，
  结果是**颜色直接变黑且不报任何错**。现在两组名字同时可用（`r/g/b/a` 是访问器，
  内部存储不动），并补上官方有、我们缺的 `toRGBA()` 与 `blendEq()`。
- **审计本身报出过两类假结论，都修了。** (1) 它只扫服务端一份 d.ts，
  客户端 `ClientAPI.d.ts` 的 39 个类型、169 个成员**从来就不在比对范围里**；
  (2) 泛型方法（`findChildByName<T extends UiElement>(…)`）名字后面跟的是 `<`，
  不被当成成员分隔符，于是整个成员从官方清单里漏掉——分母错了，覆盖率反而虚高。
  另外单双字母名用整词正则恒真（`spectator:` 里就含 `r`），短名现在只认"成员位置"。
- **网页体验版按空格不能加速。** 键盘的 Space / Ctrl / F 从来不会触发官方 `player.onPress`
  事件——只有触屏虚拟按钮会。官方赛车模板判定"消耗加速道具"用的正是
  `button == GameButtonType.JUMP`，所以**桌面端玩家吃了道具也按不出加速，只有手机能触发**。
  现在键盘三个键都会 fire `onPress` / `onRelease`，并且长按连发不会重复触发。
- **上车后车壳糊满整屏、什么都看不见。** 第三人称相机的避让射线打在了**玩家自己的实体**上
  （实测命中距离 0），于是镜头被夹到 0.22 格的贴身下限。官方赛车模板按 E 之后会
  `entity.mesh = 汽车` 并 `player.scale = 0.13`，正好触发这条路径；上车前是 8.5 格、
  上车后塌成 0.22 格。相机本来就不该被它正在跟随的那个实体挡住。
- **新建地图不能跳、还每 tick 刷报错。** 官方赛车脚本里有一句
  `entity.player.jumpPower = 0`（跳跃键改成了吃加速道具），而"新建世界"只清了实体、
  模型、区域、商品、出生点，**没清脚本**，于是这句渗进了每一张新图。
  顺带修掉因此暴露的三个悬空状态：脚本面板的下标指向已不存在的文件（一编辑就抛异常）、
  文件列表还挂着上一张图的 `index.js`、以及"删除第一个脚本"因为下标 0 被当成 falsy
  而从来静默失效。
- **没同意使用素材就保存地图，会把实体清零。** 官方模型读不到时，194 个带网格的实体
  既进不了场景模型、又被保存时的过滤条件丢掉。现象是"我只是没同意用素材，地图没了"。
  现在改成按"有没有被某个已放置模型代表"判断，读不到素材不会再毁数据。
- **从 .dmg 打开直接崩溃。** 挂载卷只读，服务端往自己目录里写存档会 `ENOENT`；
  现在会退到 `~/.dao3-editor` 并在启动时打印实际数据目录。

### 新增

- **补上一批官方有、我们此前是空壳的脚本接口**，全部在真实浏览器里跑过：
  - `storage.getDataStorage(name)` / `getGroupStorage(name)` —— 按名字开一块键值库，
    带 `get / set / update / increment / remove / destroy / list`；`list` 返回官方那个
    可翻页的 `QueryList`（`getCurrentPage()` / `nextPage()` / `isLastPage`）。
  - `rtc.createChannel(id)` —— 频道对象支持 `add / remove / getPlayers / setVolume /
    getVolume / publishMicrophone / unpublish / destroy`。本地没有语音服务器，
    但成员集合、每人音量、麦克风授权状态是真的，且音量确实作用在该玩家的音效增益上。
  - `analytics.sensor.init(url)` / `track(distinctId, eventName, properties)` ——
    埋点记在内存并打到运行控制台，**不发往任何网络**（这是离线实现的边界，不是漏写）。
  - `gui.init / show / remove / getAttribute / setAttribute / onMessage` —— 老一套 GUI 桥，
    声明的面板会真的并进 HUD 控件层，改属性能看到 DOM 变化。官方自己就注明不建议再用，
    但老地图还在调，所以按语义实现而不是留空。
  - 购买成功事件补上官方字段 `orderId`（自增单号）与 `userId`。
- **API 参考页**（`docs/api-reference.md` → 文档站「API 参考」）。125 个类型、1044 个成员，
  逐个给官方签名、中文说明、参数逐条与单位，并标出本地是已实现还是缺口。
  这页是 `npm run build:api-ref` 从官方 `GameAPI.d.ts` / `ClientAPI.d.ts` **生成**的，
  不手写——手抄契约一定会抄错，而且抄错了看起来完全正常。
  官方那份 d.ts 把 TypeScript 自带的 ES 标准库也打了进来（`Array` / `Promise` / `WeakMap` 等
  111 个类型占了 278 KB），生成器按名字把它们排除，只留 DAO3 自己的 API。
- **应用内游玩手册**（`/manual`）。工作台左侧、编辑器顶栏、运行模式 HUD 各有一个入口，
  讲键位、一场比赛的五个阶段、加速道具怎么吃怎么用、掉出世界、常见故障。
  原来那个写着「API」的按钮指向的是 `/api/worlds` 这个裸 JSON 接口——现在「手册」给玩家看，
  「API」给写脚本的人看，两个都各得其所。
- 脚本把 `jumpPower` 设成 0 时，按跳跃会给一条提示说明**是这张地图的脚本关的**，
  不再让人对着毫无反应的按键猜。官方行为本身一个字没改。
- 请求一个模型里根本不存在的动画时，运行控制台会说明一次（按网格名去重）。
  官方 `.vb → gltf` 的转换没有保留动画轨道，20 个模型全部 0 条轨道——
  我们**不伪造动画数据**，但也不静默。
- 官网支持手机端；新增独立文档中心页与《网页体验版》《更新日志》两篇。

### 回归

- **先修了两个"审计说没问题"的假象。** 静态审计原来只扫服务端一份 d.ts，
  客户端 `ClientAPI.d.ts` 的 39 个类型、169 个成员**从来就不在比对范围里**；
  而单双字母的成员名用整词正则恒真（`spectator:` 里就含 `r:`），
  所以上面那条 `color.r` 缺失被审计报成了"已实现"。
  现在审计覆盖两份声明共 71 个类型 / 751 个成员，短名只认"成员位置"：属性访问、访问器声明、对象字面量的键。
- 端到端测试加了**真运行时**的 API 对账：核心四类的 329 个成员在浏览器里逐个探面，
  要求缺失 0、种类错（方法被实现成属性）0。文档"本地"列因此对这四类是实测结论，
  其余类型仍标注为静态比对。
- 断言从 112 条增加到 **193 条**（内核契约 77 · 端到端 52 · 体验版 43 · 发布物 21），
  全部驱动真实浏览器。新增覆盖：键盘 `onPress` 触发、上车后相机距离、
  换 mesh 与收起本体、加速道具消耗与复原、新图不继承脚本、角色六部件契约与染色语义、
  手册页在 390px 下不溢出且站内链接不死、工作台与运行 HUD 的入口、
  数据空间翻页、RTC 频道音量、GUI 面板画出与撤掉、购买事件 orderId。
- 测试不再写死服务端口。此前 e2e 写死 5173、runtime_parity 写死 5174，
  而 5173 现在是另一个项目的 dev server（本项目正确地没有去杀它、顺延到了别的端口），
  于是测试对着别人的服务跑并报出一堆假故障。现在读 `run.out` 并用 `/api/whoami`
  指纹确认是本项目，找不到就 SKIP 而不是 FAIL。
- 官网截图工具不再写死免责声明版本号。它曾把版本号写死成 `"1"`，
  声明升级后 10 张截图**全部变成那张遮罩**——而且每张字节数一模一样，不细看根本发现不了。

---

## [1.0.7] - 2026-09-23

### 变更

- **默认角色外观重做。** 原来是无脸无发的六块居中盒子。现在每个部件改成**关节 Group**
  （原点放在肩与髋），里面 33 个子网格做出头发、眉毛、眼白与瞳孔、鼻尖、嘴、领口、
  开襟阴影、腰带与铜扣、袖口、露出的小臂与手、分层的鞋与鞋底。摆臂摆腿因此是绕关节转，
  不再像钟摆绕盒子中心转。六个命名部件契约不变，18 个官方挂点、可穿戴、分部位换肤、
  `player.scale` 全部照旧生效。
- 顺带修掉一个让 `player.color` **从来没生效过**的错误：官方默认值是 `[1,1,1]`（＝不染色），
  而实现直接用它替换上衣颜色，于是蓝衬衫每次都被冲成灰白。改成叠乘；
  `skin[part]` 是具名换肤，仍按替换语义。头发/眼睛/腰带/鞋不参与染色，所以不会被涂没。

### 修复

- "按空格没反应"：见 [未发布] 里"新建地图不能跳"一条的根因说明。

---

## [1.0.6] - 2026-09-22

### 变更（素材分发策略第二次调整）

- **官方赛道模型与音效改为随包分发**：20 个 `.gltf`（2.4MB）与 40 个 `.mp3`（1.2MB）
  进入仓库 `official-project/racing-assets/`。这是仓库所有者的分发决定，
  **不构成任何许可授予**——著作权仍归 box3lab，不在 Apache-2.0 或任何开源许可之下。
- 因此默认**不放行**：闸门做在服务端，未确认前 `/assets/racing/models/*` 与
  `/data/assets/audio/*` 一律返回 `403` 并说明原因。确认框列明三项内容与各自体积，
  数字从包里现读而不是写死。
- 地图授权与素材授权**分开记录**：旧包上只同意过地图的人，不会顺手把模型和音效也放了。
- 网页体验版同步带上素材。静态托管没有服务端可用来做闸门，确认点改为首屏那张卡
  （列明来源、著作权，以及"点「进入体验」即表示你确认有权使用"）。
- 免责声明实质变化，确认版本升到 2，让已经点过"进入"的人重新看到与现状一致的那一版。

### 修复

- 体验版地图解压改按**首两个字节**判断是否需要 gunzip，而不是信 `Content-Encoding` 响应头：
  Pages 前面的 CDN 有可能已经替浏览器解过一层，再把纯 JSON 喂给 `DecompressionStream`
  会格式错、整页打不开，而这条路径在 localhost 上永远复现不出来。
- 手机端读数条撑满整屏（`position:fixed` 只写了 `top` 没写 `bottom:auto`），
  状态读数被甩到屏幕正中压住准星。
- 便携包与 npm 包带上体验版源码，否则从 npm 安装的人跑不了文档里教的 `build:play`。

---

## [1.0.5] - 2026-09-22

### 新增

- **网页体验版** <https://deepseekv5.github.io/dao3play/>：不装 Node、不下载包，
  浏览器里直接跑官方赛车模板（152 万格体素、199 实体、两个官方脚本真实执行），
  手机端左摇杆加七个虚拟键。零服务端，纯静态。
- 运行时模块**不靠文件清单**：从入口出发沿 `import` / `import()` 递归求闭包，
  只拷贝真正被引用的文件，编辑器与 VOXA 因此自然落选，不会出现清单和代码不同步。
- 发布前分发审计从三处增加到四处（git 树 / npm tarball / 便携包 / `play/`），
  公开托管那处尺度更严：只允许地图一个例外，第二份 `.gz` 都不许有。

---

## [1.0.4] - 2026-09-22

### 新增

- 官网新增**独立文档中心页**，十篇文档按「入门 / 内核 / 交付」三组排开，侧边栏常驻。
- 官网新增**依赖清单**与**致谢**页；每个页面都能返回主界面（尤其是编辑器）。

### 修复

- VOXA 编辑器界面按官方五区布局重做：左侧竖排工具栏随模式切换内容，
  体素模式 11 个工具与官方一致；物理模式下碰撞盒不给旋转（官方就没有）。
- `[hidden]` 被作者样式里的 `display:flex` 盖掉，导致 VOXA 顶栏分组该藏的时候藏不住。

---

## [1.0.3] - 2026-09-21

### 新增

- **macOS `.dmg` 与 Windows 安装程序 `.exe`**（Inno Setup 6 只在 CI 的 windows-latest 上编译
  —— Inno 没有 macOS 版，在 mac 上"生成" `.exe` 只能是假的）。
- 发布到 **GitHub Packages**：`npx @deepseekv5/dao3-editor-clone` 直接起服务。
- 便携包 zip 保留不变。

### 修复

- `.dmg` 里的启动脚本丢了可执行位（`actions/upload-artifact` 不保留 Unix 权限位）。
- npm 发布步骤改成可重入：先 `npm view` 探测，已存在就跳过，不再因 E409 卡住整条流水线。
- Release 资产名改用 ASCII——GitHub 会把非 ASCII 的资产文件名剥掉字符，
  导致下载链接指向 `DAO3-.-v1.0.0.zip`。

---

## [1.0.2] - 2026-09-21

### 修复

- **仓库缺 three.js 导致 clone 出来白屏。** `.gitignore` 里一句没锚定的 `vendor/`
  把随包分发的 MIT three.js（`public/vendor/`）一起吞掉了。改成 `/vendor/`，
  并给分发审计加上"运行时必需文件逐个确认在场"的反向断言——只查"不该在的不在"
  是不够的，这个 bug 恰恰是"该在的不在"。
- **npm tarball 会泄漏 60 个未授权素材。** `package.json` 的 `files` 里写一句 `"public"`
  就会把 `public/assets/` 整个吞进去，而 `files` 存在时 **npm 不看 `.gitignore`**。
  改为显式枚举。
- CI 抓到的两个安装包缺陷（权限位、版本号注入）。

---

## [1.0.1] - 2026-09-21

### 变更

- 版本号改为**单一来源**（`package.json`），下载链接在构建期注入。
  此前 `1.0.0` 散落在六处文档与脚本里，每次发版都要手改，必然漏。

---

## [1.0.0] - 2026-09-21

首个版本。DAO3（神奇代码岛 / dao3.fun）编辑器与运行时的**本地独立兼容实现**：

- 官方 `GameAPI.d.ts` 的 **329 个可调用成员**逐个对账，官方写法的脚本零改动可跑。
- 官方口径的物理：64ms/tick（20 TPS）定步长、`walkSpeed 0.22 格/tick`、重力 `-0.1`。
- 体素编辑（放置/擦除/选择/复制/粘贴/镜像/旋转）、区域触发器、商城、
  昼夜与天气、粒子、客户端 UI 树（`ui` / `input` / `screen` / `Audio`）。
- **VOXA 体素模型编辑器**：部件、骨骼、动画、物理盒、换肤、V 口令分享码。
- 官方项目包 `.zip` 全量导出与回环导入；地图数据与 Unity 侧 `.gz` 格式互通。
- 零第三方运行时依赖（只用 Node 内置模块），three.js r160 以 MIT vendored。
- 便携包、Windows 与 macOS 启动脚本、GitHub Pages 介绍站与开发文档。

### 已知边界（截至 1.0.0，部分仍成立）

- 官方 `.vb → gltf` 的模型转换**没有保留动画轨道**，20 个模型全部 0 条轨道，
  因此赛车模板的 `run` 动作播不出来（车轮不转）。仓库里没有 `.vb` 源文件，
  凭现有材料补不回来；我们不伪造动画数据，只把"要不到动作"这件事说清楚。见 1.0.6 之后。
- 本地单端：没有多人联机，`player.kick()` / 跨世界传送等是显式空实现。
- Windows 支持是**审计 + 显式实现**，未做实机验证，文档里把这两类结论分开写。
